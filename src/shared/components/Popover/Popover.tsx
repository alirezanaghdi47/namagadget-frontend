// libraries
import {
  Popover as NextPopover,
  PopoverTrigger as NextPopoverTrigger,
  PopoverContent as NextPopoverContent,
} from '@nextui-org/react';

export const Popover = NextPopover;

export const PopoverTrigger = NextPopoverTrigger;

export const PopoverContent = NextPopoverContent;
