// libraries
import {
  Modal as NextModal,
  ModalBody as NextModalBody,
  ModalFooter as NextModalFooter,
  ModalHeader as NextModalHeader,
  ModalContent as NextModalContent,
} from '@nextui-org/react';

export const Modal = NextModal;

export const ModalContent = NextModalContent;

export const ModalHeader = NextModalHeader;

export const ModalBody = NextModalBody;

export const ModalFooter = NextModalFooter;
